apiVersion: v1
kind: Service
metadata:
  name: payment-api-service
  namespace: default
  labels:
    app: payment-api
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: payment-api
