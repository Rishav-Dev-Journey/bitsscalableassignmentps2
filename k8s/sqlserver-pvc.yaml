apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mssql-data-claim
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 8Gi
  storageClassName: managed-premium
